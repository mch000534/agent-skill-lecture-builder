# Vibe Coding 工作坊：實作任務清單

姓名：____________________　日期：____________________　專案名稱：____________________

依序完成；前一項的驗證通過後才進入下一項。

1. [x] 建立語意化頁面骨架
   - 完成證據：`index.html` 包含表單、篩選導覽、訊息與清單區域。
2. [x] 先寫標題驗證失敗測試
   - 完成證據：`todo.test.js` 先因缺少 `todo.js` 出現 `ERR_MODULE_NOT_FOUND`。
3. [x] 實作標題驗證
   - 完成證據：`validateTitle` 通過型別、必填及 120／121 邊界測試。
4. [x] 先寫新增、切換、刪除與篩選測試
   - 完成證據：`todo.test.js` 覆蓋成功、失敗、不突變與新陣列契約。
5. [x] 實作純待辦邏輯
   - 完成證據：`todo.js` 匯出五個純函式且 Node 測試通過。
6. [x] 串接 DOM 與安全文字渲染
   - 完成證據：`app.js` 使用 `createElement`、`textContent` 與 `replaceChildren` 完成記憶體 UI。
7. [x] 先寫儲存邊界失敗測試
   - 完成證據：`storage.test.js` 先因缺少 `storage.js` 出現 `ERR_MODULE_NOT_FOUND`。
8. [x] 實作 localStorage 讀寫
   - 完成證據：`storage.js` 隔離讀寫例外、驗證完整資料契約，且 `npm test` 通過。
9. [x] 完成瀏覽器黃金路徑
   - 完成證據：Chromium 實測 CRUD、重新整理持久化、損壞資料、寫入失敗、XSS 與焦點行為。
10. [ ] 完成安全與權限檢查
    - 完成證據：_______________________________________________________________
